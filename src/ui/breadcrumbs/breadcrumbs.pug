include layout/utils

mixin breadcrumbs(props)
    -
        props = Object.assign(['Главная', 'О компании'], props || [])
    .container.breadcrumbs: nav: ul
            each item in props
                li.breadcrumb
                    +anchor=item
            li.breadcrumb._back
                +anchor='Назад'